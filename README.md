# Office Dashboard

Both useful and Not

## How to run

```bash
# Install python
sudo apt install python3 python3-venv

# Create venv
python3 -m venv .patch

# Activate venv
source .patch/bin/activate

# Install requirements
pip3 install -r requirements.txt

# Run
python3 temp_logger_webserver.py
```